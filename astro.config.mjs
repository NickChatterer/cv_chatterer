import { defineConfig } from 'astro/config';

// Замени 'your-username' и 'your-repo' на свои данные
// Например: site: 'https://chatterer.github.io', base: '/portfolio'
// Если репозиторий называется username.github.io — base убери совсем
// export default defineConfig({
//   site: 'https://your-username.github.io',
//   base: '/your-repo-name',
// });


export default defineConfig({});